{
  "identifier": "default",
  "description": "Default permissions for ARBOR",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-stdin-write",
    "shell:allow-kill",
    "updater:default"
  ]
}
